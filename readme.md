# Agenda de Contactos - Examen Parcial

**Descripción:** Aplicación CRUD de una agenda de contactos. Backend con Node.js/Express y MongoDB (Mongoose). Frontend en HTML/CSS/JS (fetch + async/await) y Bootstrap.

## Estructura
- `backend/` - servidor, modelos, controladores, rutas.
- `frontend/` - cliente web (index.html, css, js).

## Requisitos
- Node.js (v16+ recomendado)
- MongoDB Atlas o MongoDB local

## Instalación y ejecución (backend)
1. Clona el repositorio:
   ```bash
   git clone <tu-repo-url>
   cd agenda-contacts/backend
